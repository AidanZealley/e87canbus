{
  "name": "e87canbus-frontend",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names coordinator,console,contracts \"pnpm dev:coordinator\" \"pnpm dev:console\" \"pnpm api:watch\"",
    "dev:coordinator": "pnpm --filter @e87canbus/coordinator dev",
    "dev:console": "pnpm --filter @e87canbus/console dev",
    "build": "pnpm --filter './apps/*' build",
    "build:coordinator": "pnpm --filter @e87canbus/coordinator build",
    "build:console": "pnpm --filter @e87canbus/console build",
    "lint": "eslint .",
    "test": "pnpm --recursive --workspace-concurrency=1 test",
    "typecheck": "pnpm --recursive typecheck",
    "format": "prettier --write \"{apps,packages}/**/*.{ts,tsx}\"",
    "api:generate": "uv run --project .. python ../scripts/generate_openapi.py && openapi-ts && node scripts/generate-button-command-catalogue.mjs && uv run --project .. python ../scripts/generate_console_openapi.py && openapi-ts -f apps/console/openapi-ts.config.ts",
    "api:check": "uv run --project .. python ../scripts/generate_openapi.py --check && node scripts/check-http-contract.mjs && node scripts/generate-button-command-catalogue.mjs --check && uv run --project .. python ../scripts/generate_console_openapi.py --check && node scripts/check-console-http-contract.mjs",
    "api:watch": "uv run --project .. python ../scripts/watch_frontend_contracts.py"
  },
  "devDependencies": {
    "@eslint/js": "^10",
    "@hey-api/openapi-ts": "0.99.0",
    "concurrently": "9.2.1",
    "eslint": "^10",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17",
    "prettier": "^3.8.3",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "typescript": "~6",
    "typescript-eslint": "^8"
  },
  "packageManager": "pnpm@9.15.1"
}
