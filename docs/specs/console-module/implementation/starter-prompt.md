# Starter prompt

```text
Orchestrate the complete implementation of the coordinator and console split.

Begin by reading docs/specs/console-module/implementation/README.md and every source-of-truth
document it identifies. Treat those documents as the approved requirements and workflow.

Create branch feature/coordinator-console-split from the current approved HEAD and record its base
in docs/specs/console-module/implementation/plan.md.

Execute every workstream through the documented workflow. Use a fresh implementation agent for
each workstream and a different fresh agent for independent review. Keep review loops bounded,
maintain the durable records, and commit only accepted work.

After all workstreams are accepted, run the documented whole-feature review. Triage findings and
return accepted corrections to their implementation owners. Complete focused closure and final
verification.

Prioritize the smallest complete implementation and preserve every non-goal and complexity
boundary. Continue autonomously. Ask only if a decision materially changes approved behavior or
architecture.

Report delivered work, verification, external validation pending and specification drift. Execute
the workflow fully; do not stop after summarizing or restating the plan.
```

