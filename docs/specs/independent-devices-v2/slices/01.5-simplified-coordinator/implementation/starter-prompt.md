Orchestrate the complete implementation of the simplified coordinator.

Begin by reading
`docs/specs/independent-devices-v2/slices/01.5-simplified-coordinator/implementation/README.md`
and every source-of-truth document it identifies. Treat those documents as the approved requirements
and workflow.

Create `feature/simplified-coordinator` from the current approved clean HEAD and record its base in
the plan. Execute every workstream sequentially through the documented implementation, independent
review, bounded remediation, closure and acceptance loop. Use a fresh implementation agent for each
workstream and a different fresh reviewer. Maintain the durable records and commit only accepted
work.

Run every review through the exact Claude Opus medium command in its packet. Treat findings as
evidence and triage them yourself. If a command fails, use the documented fresh-session fallback and
record the substitution.

After all workstreams are accepted, run the documented whole-feature review and final verification.
Delete obsolete behavior and machinery completely, preserve the four retained coordinator
responsibilities, and add no future vehicle-output framework.

Continue autonomously. Ask only if a decision materially changes approved behavior or architecture.
Report delivered work, verification, specification drift and deferred Optional observations.
Execute the workflow rather than stopping after restating it.
