# Starter prompt

```text
Orchestrate the complete implementation of Raspberry Pi image building.

Begin by reading docs/specs/raspberry-pi-image-building/implementation/README.md and every
source-of-truth document it identifies. Treat those documents as the approved requirements and
workflow.

Create feature/pi-image-building from the current approved HEAD and record its base in the plan.
Execute every workstream in order with a fresh implementation agent and a different fresh reviewer.
Keep review loops bounded, maintain the workflow records and commit only accepted work. Pause at the
two documented MacBook and Raspberry Pi checkpoints, record Aidan's results, then continue.

After all workstreams are accepted, run the whole-feature review. Triage findings, return accepted
corrections to their original owners, complete focused closure and run final verification.

Prefer the smallest complete implementation and preserve all non-goals. Continue autonomously. Ask
only if a decision materially changes approved behavior or architecture.

Report delivered work, verification, hardware evidence, pending checks and specification drift.
Execute the workflow rather than stopping after restating it.
```
