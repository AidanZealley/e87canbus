"""Device vocabulary and the controller-owned registry of their lifecycles.

- ``catalogue`` - device roles, sources and their expected protocol versions.
- ``registry``  - the pure HELLO/HEARTBEAT lifecycle transitions for each device.
"""
