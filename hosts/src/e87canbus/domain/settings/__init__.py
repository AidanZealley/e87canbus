"""User-configurable application settings and their storage boundary.

- ``values``     - the settings themselves, with validation and defaults.
- ``repository`` - the durable-storage boundary settings are loaded and saved through.
"""
