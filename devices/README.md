# Devices

The coordinator panel is the only firmware project currently checked in. The old button-pad and
Servotronic CAN firmware projects have been removed. Later slices add independent devices with
their own configuration and control paths.
